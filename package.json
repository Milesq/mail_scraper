{
  "name": "mail-scraper-config-www",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "parcel serve www/*.html --no-source-maps",
    "build": "parcel build www/*.html --no-source-maps -d build/static --no-content-hash --no-minify",
    "build:release": "parcel build www/*.html --no-source-maps -d build/static --no-content-hash"
  },
  "keywords": [],
  "author": "Milesq <wmilosz88@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "parcel-bundler": "^1.12.4",
    "parcel-plugin-wasm.rs": "^1.2.16"
  }
}
